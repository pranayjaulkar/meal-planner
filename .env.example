# MongoDB connection string for local development or production.
MONGODB_URI=mongodb://localhost:27017/meal-planner

# Secret used for signing authentication tokens.
JWT_SECRET=replace-this-with-a-secure-random-secret

# Optional: next app environment mode. Defaults to development when not set.
NODE_ENV=development

NEXT_PUBLIC_APP_URL=https://localhost:3000